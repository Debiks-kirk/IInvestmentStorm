# 进度日志

> 按时间倒序记录可核查的实质进展。最后更新：2026-08-05（夜行者藏品优先）。

相关文档：[项目记忆](memory.md) · [计划](plan.md) · [技术基线](tech.md) · [调研](research.md)

## 2026-08-05

- 补全夜行者结算反馈：双影下注的下次私密弹窗会明确写出采用 A 时“比 B 少投入 X 金币”、采用 B 时“比 A 多投入 X 金币”，并同步给出排名净收益高／低的差额；终局逐轮复盘显示同一比较。验证：夜行者回归测试 21 项通过，npm run build 通过。下一步：线下确认藏品优先导致“多投入但净收益较低”的文案是否足够直观。

- 完成夜行者“双影下注”的藏品优先选项：发动弹窗默认开启“优先拿藏品”，仅 A/B 中一档能获得拍品时，系统即使面对较低的金币净收益也会采用该档；玩家可以私密关闭，恢复原有“排名奖励 − 实际下注”比较。模拟只读取排名第一的拍品归属，绑匪随后可能成功的抢夺不参与夜行者预判。终局逐轮复盘会列出 A/B 是否得拍品和实际采用原因；Bot 会按其收藏倾向决定是否保留该偏好。验证：完整单元测试 102 项通过，npm run build 通过。下一步：线下试玩确认“优先拿藏品”的默认选择是否符合直觉。

- 完成配置导入／导出与开局页美化：已保存预设可导出为版本化分享文本或 `.json` 文件；导入支持粘贴及选择文件，安全解析后作为新本机配置保存并立即载入，不携带对局进度或私密数据。基础开局页改为更轻量的座位、轮数、金币与“高级规则”入口；奖励、道具、身份、动画等移入可滚动弹窗，窄屏为单列且可独立关闭。验证：`npm run test -- --run` 101 项通过，`npm run build` 通过。下一步：线下确认社区分享文本在常用聊天软件中的复制体验。

- 新增身份高级配置：开局身份可调为 2–5 选 1；预言家、绑匪、小偷、逆转者、说客、商人和夜行者均可设置每局可用次数。为保持原有默认平衡，前五类默认为 12 次，商人与夜行者均为 2 次。会话升至 v17；旧存档和预设会补齐新字段且不改写已发生结果。私密技能区显示剩余次数，提交校验和 Bot 计划器会共同阻止次数耗尽后继续发动。验证：`npm run test -- --run` 99 项通过，`npm run build` 通过。下一步：线下试玩确认 3–5 选 1 在不同人数下的身份辨识与阅读负担。

- 修复移动端余额长按偶发失效：余额按钮现在在按下时安全尝试捕获指针，并在松手、取消、丢失捕获、鼠标离开或失焦时统一收起；同时用 `touch-action: none` 阻止浏览器把按钮上的长按识别成页面手势并提前取消。新增独立余额按下／松开烟测。下一步：真机连续操作确认滚动页面后仍可查看余额。

- 调整预言家默认平衡：新建对局与系统预设中“观身份”费用从 5 金币改为 3 金币；观财、观星仍免费。已有对局／预设因保存了完整身份配置而继续使用原值。Bot 付费推演门槛同步读取当前配置而非硬编码金额。验证：默认设置回归测试、完整单测与生产构建通过。下一步：线下观察 3 金币是否让观身份的使用频率更合适。

- 修复说客任务弹窗关闭后信息不完整的问题：私密操作栏顶部的任务邮箱现常驻显示任务名称、具体完成条件、比较对象姓名（如有）与未完成付款；高于／低于任务会把“指定玩家”替换为实际名字。窄屏下条件和违约金额改为纵向排列，避免被截断。下一步：线下确认任务邮箱在多任务叠加时仍易读。

- 优化道具竞购的私密回执：得标者直接收到“恭喜你，竞拍到了道具「名称」”；其他参与者直接收到“很遗憾，你的出价不足或出现并列出价，没能拍到道具「名称」”。商人发起竞购时仍只收到成交／流拍经营信息，不会因规则锁定的 0 报价被误导为竞购失败。下一步：在线下试玩观察回执是否足够快地让新玩家理解唯一报价规则。

- 完成 Bot 决策多样化与反并列升级：会话升至 v15，每名 Bot 获得随“会话 ID + Bot ID”固定的隐藏行为倾向，刷新不变、重开局重新生成，复仇局仅继承恩怨。竞拍与道具竞购均改为枚举全部合法半金币报价；对手以公开历史、现金区间和合法已提交情报形成 11 点报价样本，评分明确惩罚预期撞价。道具、身份、预测与下注进入统一计划评分，Bot 也会主动处理预言家推演与改拍令候选。验证：新增 100 个公开局面模拟、竞购报价、v14→v15 行为迁移与复仇局重抽倾向回归；`npm test -- --run` 97 项、`npm run build` 及 `SMOKE_ONLY=bot npm run test:smoke` 通过。全量烟测仍在既有新手引导的“启动抽奖机”等待处超时，Bot 流程未受影响；下一步：独立修复该新手烟测等待条件，并用真实全 Bot 对局观察长期平衡和人类可反制性。

- 完成预言家、命运硬币与竞购回执调整：观财／观星免费，只有观身份支付设置中的费用；观身份猜对或猜错会立即进入本人私密通知队列。命运硬币在翻面时立刻扣/加余额并锁定道具，负面结果会把当前下注自动压低到可用余额内；会话升至 v14，刷新不会重掷。回合结果会复盘硬币效果，但不再二次改变余额。移除了拍品私密得失回执和终局逐人确认，保留每位玩家的道具竞购成交／流拍回执。验证：`npm test -- --run` 94 项通过；`npm run build` 通过。下一步：在真机检查硬币翻面后下注数字的即时变化与通知队列阅读节奏。

- 完成说客技能弹窗重做：首层只保留“随机发布／指定发布”两张大卡；指定发布进入第二步再展示任务卡，并提供返回与取消。人物卡页显示任务、基础费用、附加费用及合计，取消不会写入任务或扣费。移动端任务卡强制单列并限制弹窗横向溢出。验证：`npm test -- --run` 94 项通过，`npm run build` 通过，`SMOKE_ONLY=lobbyist npm run test:smoke` 通过（涵盖随机、指定、返回、目标／比较对象和 360px 无横向溢出）。全量 `npm run test:smoke` 两次均在既有 `runGame` 终局等待“全局结束”处超时，未进入本次说客流程；该问题待独立排查。下一步：在真机确认两步卡片的首眼理解速度。

- 完成圆桌轮转传递：玩家座位顺序不再重排，第 N 轮从座位 `N mod 玩家数` 开始，随后沿固定座位环依次操作；每人恰好一次后才进入揭晓。道具竞购使用同一轮起点，抽奖揭晓页会提示本轮首位玩家；首位获得“偷看底牌”时的改抽规则也改读轮转起点。新增 3/6 人顺序单测，并修正浏览器流程中“第二轮固定从 1 号开始”的旧假设。验证：`npm test -- --run` 94 项通过，`npm run build` 与 `npm run test:smoke` 通过。下一步：线下圆桌试玩确认主持人能否仅靠首位提示顺畅传递设备。

- 预言家改为主动“天机推演”：每回合支付可配置默认 5 金币，私密选择观财、观星或观身份。观财显示不带姓名、必含真实余额的开局最高/最低模糊区间；观星最多展示未来两轮的冻结预设拍品；观身份限制每回合一次、组合不可重猜和猜对玩家不可再猜，猜中直接获得一张循环道具，猜错仅损失费用。所有随机结果和开局余额快照已写入 v12 存档，刷新不改变结果；Bot 不再免费读取预言牌堆。
- 新增预言家纯逻辑回归与 v11→v12 迁移覆盖；`npm test -- --run` 通过 92 项，`npm run build` 与 `npm run test:smoke` 成功。下一步：线下试玩观察 5 金币推演成本和身份猜测的命中体验。

- 收藏家实际获得其开局选定类别的拍品后，在所有常规结算完成后额外获得 5 金币；该金额写入私密身份收支和既有通知队列，公开账本不显示。原有终局固定资产“额外计为 1 件”的效果仍保留，新奖励不改变预测付款等既有结算顺序。新增同类与非同类获奖单测；`npm test -- --run` 通过 88 项，`npm run build` 与 `npm run test:smoke` 成功。下一步：线下试玩观察该奖励的强度。

- 澄清说客发动技能的选择页：首层改为“随机发布”和“指定发布”两张语义区域，分别明确“不额外支付”和“额外支付 5 金币”，指定任务卡只出现在后者，后续人物/比较对象卡流程不变。操作倒计时新增独立开关，所有新局、系统预设、旧对局和旧自定义预设默认关闭；开启后仍使用既有 20 秒与可恢复绝对截止时间。会话升为 v11，v10 存档会清理旧截止时间以避免无意启动。验证证据：`npm test -- --run` 通过 86 项，`npm run test:smoke` 通过，`npm run build` 成功。下一步：线下观察随机/指定两块的首次理解速度。

- 完成操作时限与目标道具交互升级：`GameSession` 升为 v10，真人竞拍与竞购使用设置化的 20 秒默认绝对截止时间；通知、发卡、选角和教学不启动时钟，刷新降级传递页仍保留截止时间。超时会提交已确认选择，未完成的目标/确认弹层不会生效；已抽取但未选定的改拍令由系统锁定首张候选，避免对局卡死。指定目标道具改为“确认使用 → 大号玩家卡选择 → 安排完成”，偷看结果以私密弹窗展示并锁定本轮使用；命运硬币改为确认后才掷出。说客按钮改为“发动技能”，免费和次数用状态标签突出。验证证据：`npm test -- --run` 通过 86 项，`npm run test:smoke` 通过（含 5 秒真人超时自动提交），`npm run build` 成功。下一步：线下观察 20 秒在不同人数局中的压力是否合适。

- 完成身份规则与私密反馈升级：赌徒的猜错、跳过罚款拆分为独立设置，新局均默认为价值 50%；道具商人以可迁移次数状态替代一次性布尔状态，默认每局两次、同回合拒绝重复，并在成交／流拍后私密反馈收入和剩余次数。身份二选一改为说客唯一、其他身份可重复；非说客在选中两次以内严格等权，同次候选永不重复，正常池不足才回退到更多重复。
- 私密页面保留所有操作区展开，将说客任务移动为顶部紧凑“任务邮箱”；身份、卡牌、目标选择与最终提交仍保持原有直接可见的流程。主动身份技能增加独立确认弹窗；通知改为任务优先、单张逐条展示，避免任务、道具和结算反馈重叠。弹窗增加遮罩淡入、卡片弹入与图标印章动画，降低动态效果仍由既有全局设置压缩。验证证据：`npm run build` 成功、`npm test -- --run` 通过 85 项、`npm run test:smoke` 通过。下一步：线下试玩观察任务邮箱在多人叠加任务时的阅读节奏。

- 修复终局逐名揭晓在中途停住的问题。根因是 `FinalResult` 的延时调度未依赖 `revealedCount`：第一次增加后，在“尚未全部揭晓”的状态内不会安排稳定的下一次调度。现在每次人数变化都会清理旧计时器并为下一位重新安排延时，直到榜单完整出现；降低动态效果仍直接显示完整榜单，“跳过揭晓”继续可用。
- 验证证据：`npm run build` 成功；`npm test -- --run` 通过 83 项；`npm run test:smoke` 通过。6 人冒烟局改为快速动画并实际等待第 6 张最终榜单卡出现，防止后续测试依赖跳过按钮而漏检自动播放回归。下一步：线下设备上确认快速与默认速度的停顿节奏。

## 2026-08-04

- 新增首页“先玩 3 轮新手引导”入口：独立会话按轮解锁，首轮讲拍品／奖励区／秘密下注／确认与并列淘汰，次轮开启预测，第三轮展示反客为主的二次确认和逆转者主动按钮。教学局固定三件拍品、关闭首轮竞购和随机发卡，不影响任何正式局配置。验证证据：`npm test -- --run` 通过 83 项，`npm run build` 成功，`npm run test:smoke` 覆盖首轮预测锁定、次轮预测解锁、第三轮道具与身份操作及移动端无横向溢出。下一步：线下试玩确认教学文案是否还能再压缩。

- 新增终局叙事层且不挤占主操作区：五张“名场面”卡（最大胆下注、最精准预言、最狠背刺／最成功反弹、最大逆转、最会收藏）由既有回合结果和固定资产计算产生，默认折叠；回合结算在最终阶段只补充一句无金额的“局势播报”，不会泄露余额或个人下注。终局底部增加“原班再来一局”和“复仇局”；两者均重置金币、身份、拍品、道具与会话记录，复仇局仅将 Bot 对公开事件形成的恩怨按座位映射到新玩家 ID。验证证据：`npm test -- --run` 通过 82 项；`npm run build` 成功；`npm run test:smoke` 通过，并显式检查全 Bot 终局的名场面与两种再开局入口。下一步：在线下试玩中观察名场面措辞是否足够有趣、默认折叠是否符合主持节奏。

- Bot 决策种子现在明确包含 `session.id`：相同 Bot 预设在不同新局会得到不同的方案抽样、正态报价偏移和命运硬币结果；同一局的会话 ID 固定，刷新不改变已作出或待作出的决策。新增同一 Bot、不同会话种子下报价发生分化的回归测试。
- 终局排名改为从最后一名开始逐张揭晓，所有名次出现后才开放身份、Bot 档案、逐轮复盘和再开一局。前三名接着按季军、亚军、冠军顺序进入独立领奖台；可点击“跳过揭晓”，降低动态效果直接展示全部。烟测在终局会主动跳过揭晓后检查完整榜单，兼容自动化与移动端布局。验证证据：`npm test -- --run` 通过 78 项；`npm run build` 与 `npm run test:smoke` 通过。下一步：线下试玩确认完整终局演出的节奏。

- 将 Bot 的普通竞拍选择改为受控随机模型：先按候选收益建立质量门槛，在可接受方案中按 softmax 权重抽取；随后使用由人格风险、难度、局势和重复打法共同决定方差的截断正态分布微调报价。这样资源相同的 Bot 不会稳定落在同一报价，但高收益选择仍显著更常出现。偷天换日、排名逆转等依赖精确排名语义的计划不参与报价扰动；伪随机种子绑定 Bot、回合和拍品，已提交行动刷新后仍可恢复。
- 验证证据：新增“同资源 Bot 种子产生多个合法报价”回归测试；`npm test -- --run` 通过 77 项，`npm run build` 与 `npm run test:smoke` 通过。烟测同时改为验证任意合法道具都能完成公开结算：被动护盾和仅私密生效的偷看卡不一定生成公共道具影响，不能再把该标题当成必然存在。下一步：线下试玩观察不同难度下的报价离散度。

- 将回合结果页升级为三段式揭晓：先展示并列核验，再自动揭晓唯一排名与获奖者，最后展开道具影响、预测、公开账本和可选余额领跑者。拍品、并列提示、获奖金币与结算面板均有简短进入动效；反弹护盾和命运硬币有对应的效果强调。点击“跳过动画”或使用快速/降低动态效果会立即展示完整结果，确保不阻断继续下一轮和全 Bot 观战。
- 验证证据：`npm test -- --run` 通过 76 项；`npm run build` 成功；`npm run test:smoke` 通过 3/6/10 人、全 Bot、刷新隐私、道具、预设与移动端回归。下一步：用真实手机试玩确认完整动画的节奏是否需要再缩短。

- 反弹护盾改为非主动、自动消耗卡。私密库存只显示“自动待命”状态，不可点击，也不计入每轮两张道具限制；当持有者首次被香蕉皮或偷天换日指定时，结算会自动消耗护盾并将该次效果反弹给使用者。同轮护盾已经消耗后，后续指定效果正常结算；消耗记录会在下一轮把护盾回收到循环卡池。
- 验证证据：新增自动香蕉皮反弹、换日反弹、同轮只反弹一次和自动回收测试；`npm test -- --run` 通过 76 项。下一步：浏览器与生产构建回归。

- 改拍令升级为“确认后六选一”：点击道具先显示风险说明，只有按下“确认并抽取 6 张”才会从未安排拍品中抽卡并立即写入存档；随后以 2×3 卡片私密选择一件替换下一轮。确认前没有候选，确认后不可取消、重抽或刷新刷结果。预言家（含 Bot）仍读取冻结的预设牌堆，所以永远看不到改写后的真实下一拍品。
- 验证证据：新增浏览器流程，断言确认前无候选、确认后恰有 6 张并可锁定选择；单元与生产构建、全量烟测通过。下一步：线下试玩确认六选一带来的决策时间是否合适。

- 丰富说客任务：新增“获得第二名”和“保持观望（实际下注为 0）”，并保留第一、不获奖及高/低下注任务。高/低下注的比较对象现在可选择说客本人；随机任务池同步覆盖全部六类任务。结算、Bot 任务评分、私密通知与终局复盘均使用同一任务定义。
- 说客主动技能改为卡片式两步流程：点击“选择任务卡”先弹出随机/指定任务卡，选中后自动切入任务对象人物卡；只有高/低下注任务会自动追加比较对象人物卡。任务对象仍不能是说客自己，比较对象仅排除任务对象。余额不足时提交按钮会明确禁用，避免安排后无法提交。
- 验证证据：`npm test -- --run` 通过 74 项；`npm run build` 成功；`npm run test:smoke` 通过，新增任务卡、第二名任务与说客本人比较对象交互覆盖。下一步：线下试玩观察零下注任务和第二名任务的出现频率。

- 新增循环道具卡“改拍令”。持有者在非最后一轮点击后，系统立刻从未安排拍品中抽取并保存 3 张候选，同时立即锁定并移除该卡；选择其中一件后才替换下一轮实际拍品，不能取消、重抽或反复试探。未完成选择时不能提交回合，刷新后仍会回到传递页并保留同一组候选和锁定状态。
- 新局新增冻结的 `prophecyDeck`，实际 `itemDeck` 可被改拍令替换；预言家与 Bot 预言家仅从冻结牌堆读取预览，因此始终显示改写前的旧拍品。存档迁移会为旧局补齐预言牌堆、待选状态和新卡；终局逐轮复盘记录改拍令实际选中的拍品。
- 验证证据：`npm test -- --run` 通过 73 项；`npm run build` 成功；`npm run test:smoke` 通过 3/6/10 人、全 Bot、刷新隐私、道具、预设与移动端流程。下一步：线下试玩确认改拍令的出牌时机与拍品价值平衡。

- 将说客任务的默认违约付款从 3 金币调整为 5 金币。默认值由 `defaultIdentitySettings` 提供，因此新局与系统预设同步生效；旧进行中对局和已保存配置携带自身值，迁移时继续保留，不会被中途改写。高级设置仍可按局调整。
- 验证：新增新局默认值断言，并更新说客违约结算断言为 5 金币。下一步：线下试玩确认较高违约成本下的说客任务发布频率。

- 修复香蕉皮首位无法使用的问题。根因是道具库存列表在判断一张“未选中”的卡是否有目标时，错误读取了当前已选卡（或默认的已提交玩家）目标集合，导致香蕉皮在第一位操作时被禁用；结算引擎本身原已支持未来目标。
- 新增 `cardTargetScope` 作为十张道具的目标范围唯一来源：偷看底牌为“已提交玩家”，偷天换日和香蕉皮为“任意其他玩家”，其余为无目标。私密可用性、目标弹层、提交校验均改读该规则。新增单元断言全部十张卡的目标范围、首位香蕉皮指定未来玩家的结算，以及浏览器中首位玩家实际打开香蕉皮两名目标卡片。
- 验证证据：`npm test` 通过 68 项；`npm run build` 成功；`npm run test:smoke` 通过并新增首位香蕉皮交互覆盖。下一步：线下试玩继续确认高组合回合的道具说明是否足够清楚。

- 调整 Bot 提交保底策略：计划未通过校验时不再提交 0 投资/跳过预测，而是保留原计划中仍合法的投资金额与预测，仅移除卡牌和身份附加动作后重试。这样既保证状态机不会卡住，也不会让偶发失效的附加技能抹掉 Bot 的核心竞拍策略。
- 验证证据：`npm test` 通过 66 项；`npm run build` 成功；`npm run test:smoke` 通过默认身份、首轮系统竞购、全 Bot 与原有多人流程。下一步：线下试玩观察实际触发时的 Bot 决策复盘。

- 修复 Bot 停留在“正在分析拍品、局势与可用技能”的死锁风险。根因是说客的任务发布费用未计入 Bot 的回合可用现金：其计划可能先下注、再在提交校验中因余额不足被整体拒绝，从而持续展示分析页。
- `BotObservation` 现提供按本局设置和身份状态计算的说客可用费用，Bot 仅在下注后仍能支付时发布任务；自动提交同时加入保底路径：若任何未来身份或道具计划未通过既有校验，Bot 保留原定的合法投资与预测，只撤销本回合道具/身份附加动作，而不阻塞整局。
- 验证证据：`npm test` 通过 66 项；`npm run build` 成功；`npm run test:smoke` 通过，且全 Bot 烟测恢复为默认开启身份系统和首轮系统竞购。下一步：线下试玩继续观察 Bot 的任务发布频率与保底回合触发率。

- 道具商人发起竞购后不再跳过自己：所有玩家都会依次获得私密报价传递页，商人本人看到明确提示且报价控件被锁定为 0，从外观上保持与普通竞购一致。竞购结算只统计正报价，商人仍按原规则收取成交款。
- 新增高级设置“首轮系统道具竞购”，新局默认开启。创建会话时会从循环卡池预留一张公开竞购卡；身份选角完成后（或身份关闭时直接）进入竞购，所有玩家可报价，唯一最高正报价者付钱给系统并获得卡；无人得标则卡回池。旧进行中存档补为关闭，避免中途插入首轮流程。
- Bot 对普通近似最优下注加入以玩家、轮次和拍品为种子的 ±0.5 金币偏移，既让相似 Bot 不再稳定报同一个数，也保持刷新/回放确定；偷天换日和任何排名逆转计划不受影响。
- 验证证据：`npm test` 通过 66 项；`npm run build` 成功；`npm run test:smoke` 通过，新增首轮系统竞购覆盖，并回归 3/6/10 人、全 Bot、身份、预设、刷新隐私和移动端。下一步：线下试玩确认首轮竞购的默认节奏与系统收款是否符合玩家预期。

- 原“刺客”显示身份现为一局唯一的“绑匪”，内部 ID `assassin` 保持不变以兼容存档。绑匪在自己的每回合可支付可配置的默认 5 金币，私密指定任意其他玩家；目标正常获得本轮拍品时，费用立即报销且藏品转给绑匪，排名奖励、预测和公开金币账本完全不变。成功会在公共结算显示“有人抢劫了本回合的藏品”；失败不公开，只在绑匪下次私密回合弹窗说明上轮损失。
- `RoundResult.itemWinnerId` 分离拍品最终归属和排名第一名，终局逐轮复盘会完整显示绑匪的目标、成功/失败和最终物品归属。旧结果迁移时安全回填为原排名第一名。
- 身份配置中的小偷目标、目标道具、绑匪与说客的任务/比较对象均改为大号玩家卡片弹层；卡片展示玩家色、名字和本次效果说明，移动端自动单列。Bot 评分进一步提升同类固定资产跳档权重，并对绑匪联合评估目标胜率、失败成本、收藏收益与恩怨；普通与绑匪近似最优计划仍按稳定种子混合，避免固定套路。
- 验证证据：`npm test` 通过 64 项（含绑匪成功、失败、物品转移、公开/私密反馈及 Bot 决策）；`npm run build` 成功；`npm run test:smoke` 通过 3/6/10 人、全 Bot、刷新隐私、道具、预设与移动端流程。下一步：线下试玩校准 5 金币抢劫成本与同类资产权重。

- 新增循环道具卡“香蕉皮”和“反弹护盾”，均会自动出现在新对局及未禁用的旧存档卡池中。香蕉皮可在私密页选择任意其他玩家；结算时目标的实际下注已扣款会返还一半，并从本轮排名完全剔除，公共结果弹窗会明确显示“某玩家下注时被香蕉皮滑倒了”。
- 反弹护盾在所有人提交后统一解析，因此不受操作先后影响：针对护盾持有者的香蕉皮或偷天换日会改为作用到使用者自身。偷看底牌仍在私密操作时即时给信息，不能由事后护盾撤回；终局逐轮复盘继续保留原始道具与目标记录。
- 验证证据：`npm test` 通过 62 项测试；`npm run build` 成功；`npm run test:smoke` 通过 3/6/10 人、全 Bot 观战、刷新隐私、道具、预设与移动端流程。烟测已明确关闭香蕉皮结算提示后继续断言结果页。下一步：线下试玩确认香蕉皮与护盾的出现频率和反制博弈是否平衡。

- 赌徒预测现采用“公开伪装、私密真实”双账本：公共预测区与公开账本将其按普通玩家显示（猜错显示普通罚款、跳过显示 `±0`、猜中显示普通收入）；实际余额仍按赌徒 50% 规则变动。下一次轮到赌徒时，私密通知会说明公开信息为伪装、实际支付/获得金额及额外奖励。
- 更新身份结算测试，确认赌徒猜错的公开 `−1.5V` 与实际 `−0.5V` 分离，跳过继续公开 `±0`、实际私密扣款，猜中保留公开普通收入并私密补充额外奖励。
- 验证证据：`npm test` 通过 59 项；`npm run build` 成功；`npm run test:smoke` 通过 3/6/10 人、全 Bot 观战、刷新隐私、道具、预设与移动端流程。
- 下一步：线下试玩确认“公开伪装”在赌徒私密反馈中的解释是否足够清晰。

- 将身份显示名由“逆行者”统一为“逆转者”；内部稳定 ID 仍为 `reverser`，不会影响旧存档、身份设置、结算动作或 Bot 策略。
- 下一步：若要让赌徒的实际身份奖惩出现在公共结算页，需要先确认这是否要打破现有身份收支保密规则。

- 修复命运硬币可试刷结果的问题：点击使用后才开始翻转；结果出现后只可确认，随后在本轮道具栏锁定，不能取消、移除或重新掷币。结算仍只读取已保存的 `coinResult`。
- 全 Bot 观战控制改为置顶、较大触控区。每轮结算会自动暂停；可选择继续自动播放，或“接管当前 Bot”（结算页接管下一位），将其改为真人并进入原有手动流程。
- 验证证据：`npm test` 通过 59 项；`npm run build` 成功；`npm run test:smoke` 通过，且全 Bot 烟测明确等待结算自动暂停后点击“继续自动”进入终局。
- 下一步：线下试玩确认自动暂停的节奏和接管按钮文案是否足够直观。

- 私密操作页新增“收到的任务”固定卡片：只对本轮被说客指定的目标显示任务、比较对象（如有）及未完成时的付款，因此首次弹窗关闭后仍可随时核对。该卡片独立于身份技能区，任务对象无论自身身份为何都能看到；发布者身份不显示给目标。
- 说客契约结算现在为双方各写一条私密反馈：目标会收到完成且无需付款，或未完成及实际付款；说客会收到目标完成且无违约款，或收到的实际违约款。逆行者仍只使用原有公共排名结算反馈，不新增重复私密通知。新增成功/失败双方事件断言。
- 验证证据：`npm test` 通过 59 项；`npm run build` 成功；复跑 `npm run test:smoke` 通过 3/6/10 人、全 Bot 观战、刷新隐私、道具、预设与移动端流程（首次全 Bot 等待超时后复跑通过）。
- 下一步：线下试玩确认私密任务卡片在多任务叠加时的阅读节奏。

- Bot 从单一候选下注升级为“回合计划”评分：先枚举正常、逆转排名卡、偷天换日和逆行者主动技能，再以实际扣款、交换后的排名投资、获奖区倒转、奖励、固定资产、任务和现金风险共同选择。逆行者会尝试以较低名次挤入获奖区并倒转为高奖励；偷天换日会优先以 0/低实际投资换取高估计投资，避免帮目标抬价。刺客按实际下注争取成功奖惩，预言家会在下一轮存在更大资产套装价值时留存现金；对手公开的同类拍品获胜记录会抬高其预计出价。
- 普通下注只在接近最优且非特殊计划时按确定性种子做小幅混合，避免固定出价规律；身份与目标道具计划仍选择明确最优方案，保证策略不会为随机性牺牲明显收益。新增测试覆盖逆行者低位倒转、偷天换日零投资、特殊计划可重复性，并修复了原信息边界测试会被随机 UUID 中的数字误触发的问题。
- 验证证据：`npm test` 通过 58 项测试；`npm run build` 成功；`npm run test:smoke` 通过 3/6/10 人、全 Bot 观战、刷新隐私、道具、预设与移动端流程。
- 下一步：真实多人试玩记录各 Bot 身份、道具和策略模式的胜率，再微调回合计划权重。

- Bot 决策升级为局势评估：从公开的总投资、最低获奖线、获奖/并列结果和公开收益变化推算对手现金区间与常见下注；候选下注按预计名次、唯一出价机会、奖励、固定资产套装、现金风险、任务和策略模式评分。落后时追赶、领先时收缩、套装临界时收藏冲刺，最后一轮允许更大胆地冲刺。
- 预测不再机械选择预计第一名。Bot 会估计对方赢面、付款能力、猜中者分摊和猜错代价；普通 Bot 仅在正期望时预测，赌徒将跳过罚款作为基线比较。新增测试覆盖公开现金估计与负期望跳过。
- 终局排行榜后新增“身份公开”，展示所有玩家的身份、简介及刺客/小偷目标或收藏家类别；Bot 档案与逐轮复盘仍随后展示。
- 验证证据：`npm test` 通过 55 项测试；`npm run build` 成功；`npm run test:smoke` 通过 3/6/10 人、全 Bot 观战、终局身份公开、刷新隐私、道具和移动端流程。
- 下一步：真实对局观察各性格的胜率和预测频率，继续微调现金估计和策略权重。

- 完成离线智能 Bot 补位：设置页座位现可逐个选择真人或 Bot，并保存 Bot 性格/难度到自定义预设；总人数保持 3–10，允许全 Bot 自动观战。会话升至 v8，旧进行中对局和旧预设安全迁移为真人座位，不中途改变规则。
- 新增 10 种策略性格与本局恩怨记忆。Bot 以裁剪后的公开/自身私密观察视图选择身份、下注、预测、两张道具、身份技能和商人竞购；高手每个正常竞拍回合只会得到一条先前已提交对手的 ±2 金币区间，不读取真实余额、身份或库存。
- 混合局自动跳过 Bot 的私密传递页；全 Bot 局会自动推进至终局，支持暂停和 1×/2×/4×。对局中不显示 Bot 性格、难度或理由；终局显示 Bot 档案及逐轮策略、合法情报和恩怨回顾。
- 验证证据：`npm test` 通过 53 项测试；`npm run build` 成功；`npm run test:smoke` 通过既有 3/6/10 人、刷新隐私、道具、预设、移动端及新增全 Bot 自动观战流程。
- 下一步：真实聚会试玩并根据胜率与玩家反馈微调各性格的竞价和复仇权重。

- 每位玩家现在每回合最多可使用两张不同道具卡。`RoundTurn.cardUses` 保存多张卡，旧 `cardUse` 记录会在 v7 迁移时转为数组；提交校验、卡片移出库存、下一轮回收、结算影响和终局逐轮复盘均按完整数组处理。私密页的每张卡需独立确认，已安排卡可点击取消。
- 新增“命运硬币”循环道具卡：私密确认弹层先播放翻转动画，约 0.9 秒后显示结果，确认后将正/反写入 `CardUse.coinResult`。正面增加 6 金币，反面从当前余额最多扣除 4 金币；结算和复盘读取持久化结果，不会重新随机。
- 验证证据：`npm test` 通过 48 项测试；`npm run build` 成功；`npm run test:smoke` 通过 3/6/10 人、刷新隐私、道具、预设与移动端流程。
- 下一步：真实多人试玩确认双道具组合的操作节奏，以及命运硬币的风险/收益是否适合默认卡池。

- 新增“逆转排名”循环道具卡，并把逆行者主动技能与道具卡统一记为 `RoundResult.rankingReversalCount`。奇数次会倒转获奖区，偶数次会恢复正常；例如技能与卡叠加时，结算页会显示“获奖区排名被逆转了 2 次，故排名不变”。会话格式升级为 v6，旧卡池会补入新卡（未禁用且不在库存时），历史回合安全补齐次数 0。
- 身份选卡与详情现在标明主动/被动及主动技能入口；私密技能区对被动身份明确说明无需点击，对逆行者、道具商人和说客的不可用按钮直接显示余额不足、技能用完、最后一轮、卡池为空或本轮已发布等原因。结算标题改为“本轮道具与排名变化”，移除了多余的匿名提示。
- 验证证据：`npm test` 通过 43 项测试；`npm run build` 成功；`npm run test:smoke` 通过 3/6/10 人、刷新隐私、道具、预设与移动端流程。
- 下一步：真实多人试玩确认逆转排名卡的出现频率与逆行者费用是否需要进一步平衡。

- 终局排行榜后的按玩家“身份档案”已替换为按轮展开的“逐轮复盘”。每轮公开所有玩家实际下注、排名下注变化、道具使用者与目标、身份技能及其实际收支、排名奖励、拍品归属、预测结果与第一名支付；原有进行中对局的私密公开策略未变。最后一轮默认展开，其他回合可点开查看，窄屏自动改为单列。
- 验证证据：`npm test` 通过 40 项测试；`npm run build` 成功；`npm run test:smoke` 通过 3/6/10 人、刷新隐私、道具、预设与移动端路径。烟测同时保留私密身份详情检查，并在身份对局终局断言新复盘标题。
- 下一步：通过真实多人试玩确认复盘信息量与折叠默认状态是否适合桌游结束后的讨论节奏。

- 偷天换日现在可选择本轮任意其他玩家，包含尚未操作者；目标选择区会明确说明在统一结算时交换排名金额。提交校验区分两张目标卡：偷看底牌仍仅允许已提交目标，偷天换日只要求目标为其他有效玩家。
- 新增“尚未操作目标”的纯结算测试，确认交换只改变排名金额、不改变实际下注；浏览器冒烟脚本改为逐条关闭私密通知和道具领取提示，避免真实弹窗遮挡交互。验证证据：`npm test` 通过 40 项；`npm run build` 与 `npm run test:smoke` 均通过。
- 下一步：线下试玩确认先手持有偷天换日时的目标选择策略和博弈体验。

- 完成身份私密反馈：预言家、赌徒、刺客、收藏家和小偷的技能区会明确说明本轮没有可主动发动的技能；逆行者、商人和说客的操作按钮仍显示当前选择和费用。
- 每轮结算后会把身份事件转成仅对应玩家可见的通知，并在其下一次私密回合弹窗展示结果与实际赚/亏金额。说客契约在下一轮开始前额外向目标排入任务弹窗，明确显示任务内容和未完成时的付款；终局曾以身份档案展示每条事件明细，后续已由逐轮复盘取代。
- 通知改为按 ID 逐条确认，道具领取不再清除同一玩家的其他未读身份提示。验证证据：`npm test` 通过 39 项；`npm run build` 与 `npm run test:smoke` 均通过。
- 下一步：线下试玩确认多条私密通知连续出现时的阅读节奏。

- 完成身份规则平衡：逆行者由被动奖励重排改为私密主动技能，可支付 6 金币倒转本轮获奖区内排名，最后两轮自动加倍为 12；未发动时使用正常排名。赌徒改为猜中额外获得价值 50%，猜错或跳过均私密扣除价值 50%。
- 说客现在默认随机生成并立即保存下一轮任务，目标玩家会在自己的私密回合看到实际任务；开启“指定任务”后才显示任务选择，且在原发布费外另扣默认 5 金币。逆行费用和说客指定加价已加入高级设置。
- 会话格式升至 v5，保留旧会话的兼容加载，并为旧契约补齐指定标记。新增自动检查覆盖逆行者的发动与末两轮费用、赌徒三种预测结果、说客随机任务、小偷上限和失败分支、存档迁移。
- 验证证据：`npm test` 通过 39 项测试；`npm run build` 成功；`npm run test:smoke` 通过 3/6/10 人、隐私刷新、道具、预设与移动端回归。
- 下一步：线下试玩中重点观察逆行者 6/12 金币费用和说客随机任务的节奏。

- 完成私密身份系统：新对局先按玩家顺序进行点击进入的二选一选角；目标、资产类别和商人初始道具均在同一私密流程中配置。身份系统默认开启，系统预设同步启用；旧会话和旧自定义预设迁移后保持关闭，避免改变进行中的规则。
- 七个身份已接入会话和结算：预言家预览下一轮拍品，赌徒独立奖惩，刺客比较实际下注，收藏家增加终局固定资产件数，小偷按公平顺序截获新道具，道具商人发起下一轮秘密竞购，逆行者重排个人奖励并秘密接收拍品，说客发布下一轮任务并在余额不足时公平分配违约款。身份影响保持私密，终局才通过完整揭示记录公开。
- 验证证据：`npm test` 通过 35 项测试；`npm run build` 成功；`npm run test:smoke` 通过，覆盖默认选角和当时的终局揭示，以及原有 3/6/10 人、刷新隐私、道具、预设和移动端路径。
- 下一步：组织线下多人试玩，依据实际体验调整身份数值和启用组合。

- 将传递页进入私密操作和私密页身份详情统一改为单击，移除长按计时与传递进度动画；余额查看仍保持长按以保护敏感金额。冒烟测试相应改为点击路径验证。
- 验证证据：`npm run build` 成功；`npm run test:smoke` 已覆盖点击传递、点击身份详情、刷新隐私保护、3/6/10 人流程、道具与移动端布局。

- 在余额旁增加身份私密入口：身份名称、背景、被动/主动技能与限制暂为明确占位。私密页另新增禁用的“身份技能”操作区，供后续身份系统接入；当前不改变数据模型、存档或结算。
- 验证证据：`npm run build` 成功；`npm run test:smoke` 使用真实鼠标长按验证身份详情的打开与关闭、技能操作区以及 3/6/10 人私密流程和移动端布局。

- 在每位玩家的私密操作页新增“我的固定资产”：按类别汇总已拍品，列出具体物品、数量以及当前已触发的终局加成；单件尚未触发时明确提示还需收集的数量。该区只读取当前操作玩家的收藏，不泄露其他玩家信息。
- 验证证据：`npm test` 通过 28 项测试；`npm run build` 成功；`npm run test:smoke` 已检查已拍品玩家的私密分类资产卡及 360px 无横向溢出。

- 将道具机制改为循环卡池：每张启用卡在被抽到后仍由持有者独占；本轮实际使用的卡会在结算完成、下一轮发卡前重新洗回卡池，未使用卡始终留在持有者库存。卡池不会因此生成同一卡的重复副本。设置说明和精简规则已同步。
- 验证证据：`npm test` 通过 28 项测试（新增卡池回洗与去重覆盖）；`npm run build` 成功；`npm run test:smoke` 通过 3/6/10 人对局、隐私刷新、道具发放、配置预设和移动端检查。
- 下一步：可将本次原子提交推送到远程仓库，Render 会按已启用的自动部署设置发布新版本。

- 将设置页的系统配置与已保存配置移至表单底部，避免预设区抢占开局时填写玩家与规则的首屏空间；保存、加载、覆盖和删除行为不变。

- 完成配置预设与固定资产扩展：新增独立版本化的本地配置库，系统提供 3 人快速、6 人标准、10 人派对预设；自定义预设会保存玩家姓名和完整高级规则，并支持加载、覆盖及删除。新对局默认猜错罚款为 1.5V、道具概率为 80%、不公开余额领跑者。
- 全部 29 件拍品已归入生活娱乐、交通旅行、奢侈科技、地产产业四类；终局以现金加固定资产的总资产排名，固定资产只在终局计算，不进入每轮余额。v3 会话迁移会为旧牌堆、收藏和历史结果补齐类别而不重算历史回合。
- 劫富济贫现在保存并匿名展示本轮总转移金额，仍不泄露使用者、最富者、最穷者或个人金额。
- 验证证据：`npm test` 通过 27 项测试；`npm run test:smoke` 覆盖 3/6/10 人流程、预设保存加载删除、固定资产终局、隐私刷新和 360px 无横向溢出；`npm run build` 成功。
- 下一步：真实多人试玩时观察固定资产轻量档位是否会过度偏向地产产业套装。

- 完成“私密道具资产”扩展：新增红卡、黑卡、偷看底牌、偷天换日、劫富济贫和反客为主六张不放回卡；从第二轮起按最低余额和概率秘密发卡，首位玩家抽到偷看卡时会重抽其他启用卡。库存、待告知记录、禁用配置和迁移均写入版本化本地存档。
- 结算引擎已区分实际扣款、排名投资和有效拍品价值，按既定顺序处理道具；结果页匿名展示道具影响、公开真实结算奖励，并默认仅展示当前余额领跑者姓名。公开账本的奖励文案已统一为“获奖”。
- 验证证据：`npm test` 通过 20 项规则测试；`npm run test:smoke` 覆盖既有 3/6/10 人流程与持卡人私密提示、目标选择、道具结果和移动端无横向溢出；`npm run build` 成功。
- 下一步：进行真实多人试玩，重点观察道具发放概率与劫富济贫的节奏是否需要调整。

- 将轮前转盘替换为抽奖机：以抽象彩球、摇奖状态和出奖槽展示随机过程，避免候选物品过多及首屏版面失衡。360px 实机截图已核对；`npm test`、`npm run build` 与 3/6/10 人冒烟测试均通过。
- 完成 React/TypeScript/Vite 纯前端首版，覆盖首页、精简规则、基础/高级设置、抽奖机、隐私传递、私密下注预测、揭晓账本和最终排行榜。
- 实现整数半金币结算引擎、并列剔除、奖励后预测结算、余额封底、公平余数分配、不重复物品牌堆和版本化本地存档。
- 验证证据：`npm test` 通过 9 个规则测试；`npm run test:smoke` 自动跑通 3、6、10 人各一局及私密页刷新保护，并断言关键 360px 页面无横向溢出；`npm run build` 成功生成生产包。
- 修复人工巡检发现的 360px 首页横向溢出；临时巡检截图已清理，自动化截图保留在忽略的 `.artifacts/` 中供本地复查。
- Render 官方文档核验 `runtime: static`、`npm ci && npm run build`、`dist` 发布目录和 SPA rewrite 配置有效。
- 局限：尚未执行真实多人线下试玩，也未实际创建远程 Render 服务。
- 功能提交：`2453f2e`（`feat(game): 实现密封竞价完整对局`）。
- 下一步：组织真实多人试玩；准备上线时连接远程仓库并由 Render Blueprint 部署。

## 2026-08-05 — 夜行者双影下注

- 完成第九张身份「夜行者」及主动技能「双影下注」：免费、每局两次；私密确认 A/B，B 必须高于 A。结算纯函数在全员提交后比较两档的排名奖励减实际下注；影价严格更高才采用 B，收益相同固定保留 A。
- 新增 v16 迁移、匿名公共结算效果、终局 A/B/名次/净收益复盘、常规与 Bot 的兼容校验。Bot 仅在估算影价的净排名收益明显更高时发动。
- 验证：`npm run test -- --run` 通过 98/98；生产构建通过。现有全量浏览器烟测在新手局首轮等待“启动抽奖机”时超时，截图显示按钮已渲染；该既有脚本失败尚未归因，夜行者专属浏览器路径仍待补。

## 2026-08-03

- 已读取并核对 `rule.txt`，确认目录此前无代码、状态文档及 Git 仓库。
- 已锁定玩法语义：下注消耗、并列剔除、余额不为负、预测多人公平分账、默认隐藏个人下注。
- 已初始化 React/TypeScript/Vite 工程、Render 静态站点配置、Git 仓库和项目状态文档。
- 初始化提交：`b6cadfc`（`chore(project): 初始化游戏工程与状态文档`）。
# 2026-08-05 — 回合反馈、主动小偷与道具生态升级

- 完成双份循环卡池、抽空补卡、中场系统竞购队列、主动小偷和全员拍品私密回执；终局榜单前新增逐人确认最后一轮回执。
- 预测入口统一为大号玩家卡片弹窗；高级设置新增中场竞购和小偷发动费用，逆转者默认费用改为 5，发卡概率默认 100%。
- 结算结果记录每轮的总资产快照，终局逆转叙事读取该快照而非只读中途现金。
- 验证：`npm test -- --run`（94 项通过）；`npm run build` 通过。
- 下一步：线下试玩验证中场双竞购的节奏与小偷费用是否合适。
# 2026-08-05 — 预测交互回退

- 按试玩反馈，将“预测第一名”从人物卡弹窗恢复为原有的直接玩家列表；道具和身份技能的目标选择仍使用人物卡片弹窗。
